<% layout('layouts/boilerplate') %>

<main class="h-fit w-full flex flex-col items-center space-y-8">
    <h1 class="text-7xl mt-10"> <i><%= tag %></i>  Trails</h1>
    <div class="h-full w-full flex flex-wrap justify-evenly items-center">
        <% for (let taggedTrail of taggedTrails ) { %>
        <%- include('../partials/trailPartial.ejs',{singleTrail:taggedTrail}) %>
        <% } %>
    </div>


</main>

